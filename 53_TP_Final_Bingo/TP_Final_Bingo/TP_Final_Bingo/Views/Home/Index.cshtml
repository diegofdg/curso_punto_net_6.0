@model List<int>
@{
    ViewData["Title"] = "TP Final Bingo";
}

<div class="container text-center">
    <div class="row">
        <div class="col">
            <table class="table table-bordered border-dark">
                <caption class="caption-top fw-bold">Cartón 1</caption>
                <tbody>
                    <tr>
                        @for (int i = 0; i < 9; i++)
                        {
                            var item = Model[i];
                            if (item == 0)
                            {
                                <td class="bg-info" style=" width:10%;"></td>
                            }
                            else
                            {
                                <td>@item</td>
                            }
                        }
                    </tr>
                    <tr>
                        @for (int i = 9; i < 18; i++)
                        {
                            var item = Model[i];
                            if (item == 0)
                            {
                                <td class="bg-info" style=" width:10%;"></td>
                            }
                            else
                            {
                            <td>@item</td>
                            }
                        }
                    </tr>
                    <tr>
                        @for (int i = 18; i < 27; i++)
                        {
                            var item = Model[i];
                            if (item == 0)
                            {
                                <td class="bg-info" style=" width:10%;"></td>
                            }
                            else
                            {
                                <td>@item</td>
                            }
                        }
                    </tr>
                </tbody>
            </table>
        </div>
        <div class="col">
            <table class="table table-bordered border-dark">
                <caption class="caption-top fw-bold">Cartón 2</caption>
                <tbody>
                    <tr>
                        @for (int i = 27; i < 36; i++)
                        {
                            var item = Model[i];
                            if (item == 0)
                            {
                                <td class="bg-success" style=" width:10%;"></td>
                            }
                            else
                            {
                                <td>@item</td>
                            }
                        }
                    </tr>
                    <tr>
                        @for (int i = 36; i < 45; i++)
                        {
                            var item = Model[i];
                            if (item == 0)
                            {
                                <td class="bg-success" style=" width:10%;"></td>
                            }
                            else
                            {
                                <td>@item</td>
                            }
                        }
                    </tr>
                    <tr>
                        @for (int i = 45; i < 54; i++)
                        {
                            var item = Model[i];
                            if (item == 0)
                            {
                                <td class="bg-success" style=" width:10%;"></td>
                            }
                            else
                            {
                                <td>@item</td>
                            }
                        }
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
    <div class="row">
        <div class="col">
            <table class="table table-bordered border-dark">
                <caption class="caption-top fw-bold">Cartón 3</caption>
                <tbody>
                    <tr>
                        @for (int i = 54; i < 63; i++)
                        {
                            var item = Model[i];
                            if (item == 0)
                            {
                                <td class="bg-danger" style=" width:10%;"></td>
                            }
                            else
                            {
                            <td>@item</td>
                            }
                        }
                    </tr>
                    <tr>
                        @for (int i = 63; i < 72; i++)
                        {
                            var item = Model[i];
                            if (item == 0)
                            {
                                <td class="bg-danger" style=" width:10%;"></td>
                            }
                            else
                            {
                            <td>@item</td>
                            }
                        }
                    </tr>
                    <tr>
                        @for (int i = 72; i < 81; i++)
                        {
                            var item = Model[i];
                            if (item == 0)
                            {
                                <td class="bg-danger" style=" width:10%;"></td>
                            }
                            else
                            {
                            <td>@item</td>
                            }
                        }
                    </tr>
                </tbody>
            </table>
        </div>
        <div class="col">
            <table class="table table-bordered border-dark">
                <caption class="caption-top fw-bold">Cartón 4</caption>
                <tbody>
                    <tr>
                        @for (int i = 81; i < 90; i++)
                        {
                            var item = Model[i];
                            if (item == 0)
                            {
                                <td class="bg-warning" style=" width:10%;"></td>
                            }
                            else
                            {
                            <td>@item</td>
                            }
                        }
                    </tr>
                    <tr>
                        @for (int i = 90; i < 99; i++)
                        {
                            var item = Model[i];
                            if (item == 0)
                            {
                                <td class="bg-warning" style=" width:10%;"></td>
                            }
                            else
                            {
                            <td>@item</td>
                            }
                        }
                    </tr>
                    <tr>
                        @for (int i = 99; i < 108; i++)
                        {
                            var item = Model[i];
                            if (item == 0)
                            {
                                <td class="bg-warning" style=" width:10%;"></td>
                            }
                            else
                            {
                            <td>@item</td>
                            }
                        }
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="container">
    <button type="button" class="btn btn-primary mt-2 mb-2 fs-4">Lanzar Bolilla</button>
    <div class="row">
        <div class="col-md mt-2">
            <p class="fs-5 fw-bold">Bolilla: <span class="fw-normal">(Número del 1 al 90)</span></p>
        </div>
        <div class="col-md mt-2">
            <p class="fs-5 fw-bold">Cartón Ganador: <span class="fw-normal">(Número del 1 a 4)</span></p>
        </div>
    </div>
</div>